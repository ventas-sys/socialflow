# 🧠 Memoria técnica — App Stock & Inventario (Distribuidora Universo)

**ÚNICA fuente de verdad del proyecto. Leer entera antes de tocar nada.**
Rama: `claude/stock-inventory-app-06rlv5` · PR #38 (draft) · Repo: ventas-sys/socialflow

## ⚠️ Entorno (leer PRIMERO en cada sesión nueva)
- **Zona horaria: Argentina (UTC-3)**. Todo lo que se le muestre al usuario va en hora argentina, no UTC: los servidores (Vercel, los cron) corren en UTC y hay que restar 3 horas. El sello de versión de la app ya sale en hora AR (`vite.config.js`); los cron `0 16` y `0 21` UTC son las 13 y 18hs AR.
- El repo `ventas-sys/socialflow` en `main` es OTRO proyecto (redes/WhatsApp, .html). La app de stock (React, `src/`) vive SOLO en la rama `claude/stock-inventory-app-06rlv5`. Si el contenedor clona otra rama: `git fetch origin claude/stock-inventory-app-06rlv5 && git checkout -B claude/stock-inventory-app-06rlv5 origin/claude/stock-inventory-app-06rlv5` y `npm install`.
- **2 proyectos Vercel del mismo repo**: `socialflow` (viejo, main, NO tocar) y **`stock-inventario`** (producción de esta app, Production Branch = la rama de stock, dominio **`stock-inventario-sable.vercel.app`**). El preview largo `socialflow-git-claude-stock-in-858296-...vercel.app` es el que usa el usuario a diario y es la Redirect URI de ML.
- **Caché PWA**: la app muestra la versión de build en el subtítulo (`v2026-...`, vite.config `__APP_VERSION__`). Desde 19/8 hay **auto-actualización**: el build emite `version.json` (plugin en vite.config, fuera del precache del SW) y App.jsx lo chequea con no-store al volver la app al frente y cada 10 min → si cambió, `reg.update()` + reload automático (guard de 1 reload/min por sesión vía sessionStorage, y reload en `controllerchange`). Si igual reportan algo "que no anda", verificar versión → Ctrl+Shift+R / incógnito.
- Deploys: cada push a la rama deploya preview + producción (~2 min). El usuario espera que le avise cuando queda verde. **Ignored Build Step (24/8)**: el proyecto stock-inventario tiene `[ "$VERCEL_GIT_COMMIT_REF" != "claude/stock-inventory-app-06rlv5" ]` (exit 0 = saltear) → solo compila NUESTRA rama; los pushes a main del proyecto viejo ya no generan builds fallidos ni mails de error. La barra flotante de Vercel (toolbar/feedback) está oculta por CSS en App.css.

## Stack y estructura
- React + Vite + PWA (autoUpdate, skipWaiting, navigateFallbackDenylist para `/api/`), Firebase Auth (Google + email/password) + Firestore, xlsx / exceljs / jszip / html5-qrcode / **leaflet** (OpenStreetMap).
- Backend serverless en `api/` (mismo repo): `api/ml/exchange.js` (OAuth ML + orders + items), `api/ml/cron.js` (descuento diario), `api/_http.js`.
- `src/config.js`: `ORG_ID='distribuidora-universo'`, `ADMIN_EMAIL='ventas@distribuidorauniverso.com'`. Todos los datos con `userId=ORG_ID` (inventario compartido). Miembros en colección `members`. **Niveles de usuario (19/8)**: master = ADMIN_EMAIL (todo); ayudantes = doc `members/{email}` con `tabs:{clave:bool}` (solapas visibles, ausente=visible) y `canEdit:bool` (false oculta crear/editar/borrar/importar en Inventario, Combos y Movimientos; Envíos y Empaque siguen operativos). Se administra en la solapa **👥 Usuarios** (solo admin, oculta en celular vía clase `.pc-only`); la solapa **ML es SOLO del master** (no aparece para ayudantes ni en el cuadro de permisos) y en Envíos los ayudantes no ven "Traer ventas" ni "Vaciar" (las ventas las trae la sesión del master; mensaje informativo en vez de "Conectá ML"); App.jsx: `myPerms`/`canSee`/`canEdit`, salto automático si la solapa actual queda prohibida. Reglas ENDURECIDAS y **PUBLICADAS en Firebase el 24/8** (`firestore.rules` del repo es la copia fiel): función `canEdit()` (admin o member con canEdit != false vía `data.get('canEdit', true)`) exige permiso real para ESCRIBIR products/combos/movements/photos; shipments/couriers/ml_orders siguen member (operación). Auditoría por movimiento.
- `firestore.rules` (publicadas): isAdmin = ventas@ **o cron@distribuidorauniverso.com**; isMember = admin o email en `members`. Colecciones: products, combos, movements, photos, shipments, couriers, ml_orders (member) y ml_accounts, settings (solo admin escribe; ml_accounts solo admin lee).

## Modelo de datos (Firestore)
- **products**: name, code (SKU), `barcodes[]` (varios; barcode=primero), category, price, quantity (puede ser negativo), minStock, location, stockType (FULL/FERRE/BASE), description, hasPhotos.
- **combos**: name, code (**SKU MLA** de la publicación), `barcodes[]`, price, location, stockType, `items[]` {productId, quantity}, itemBarcodes[], hasPhotos. Un combo = N unidades de producto(s) base.
- **photos/{id}**: dataURLs aparte, on-demand (LazyThumb + cache).
- **movements**: productId, productName, type entrada/salida, quantity, reason, reference, userName/userEmail, date.
- **shipments**: code (shipmentId ML), packId, recipient, address, lat/lng, status (pendiente/armado/camino/entregado/demorado/**archivado**), courierId/courierName, zone, courierPay, buyerPay, timestamps (armadoAt/assignedAt/deliveredAt/demoradoAt/archivedAt), account (full/ferre).
- **couriers**: {name}. **ml_accounts/{full|ferre}**: clientId, clientSecret, tokens, expiresAt, lastSyncAt. **ml_orders/{cuenta_orderId}**: órdenes ya descontadas (no repetir).

## Funciones principales
- **Iconos (28-29/8)**: combos = 🧩 (pieza de rompecabezas, antes 🎁) y productos = 🏷️ (etiqueta, antes 📦) en solapas, chips de filtro, tarjetas, miniaturas sin foto, empaquetado y reportes. Solapa **Inventario = 🏭** y solapa **Empaque = 🗳️** (emojis elegidos por el usuario, mandados por captura de WhatsApp; no inventar otros). La caja 📦 queda solo en el título de la app y en el estado Armado de un envío.
- **Búsqueda de códigos — `src/utils/refMatch.js` (28/8)**: buscador ÚNICO por código/SKU/código de barras usado en import de combos, import y medidas de Inventario, Empaquetado, solapa ML y (copiado, no importado) el cron. Excel guarda `0558` como el número 558 y se pierden los ceros → los armados no matcheaban ("No se encontraron estos códigos: 558, 676, 353..."). Si no hay coincidencia exacta compara los códigos NUMÉRICOS sin ceros de adelante y solo acepta el resultado si queda **un único candidato** (0075 vs 75 → no adivina, lo reporta como no encontrado).
- **Inventario**: productos+combos unificados, filtros, búsqueda por nombre/SKU/cualquier código de barras/ubicación/tipo + escáner con pitido; tarjeta destacada con foto y ubicación naranja; salud de stock (utils/stock.js: consumo 60 días → Sin stock / Bajo ≤20d / Saludable 21-75d / Sobre >75d / Sin ventas) en tabla, tarjeta y panel "Se terminan primero" del Dashboard.
- **Import/Export Excel**:
  - Productos: agrupa por SKU → varios códigos de barras por producto; fotos pegadas en el Excel; actualiza existentes.
  - Combos (formato catálogo real): `SKU` MLA = combo, `Codigo (ARMADO P)` = producto base, `ARMADO S` = cantidad, filas repetidas = códigos de barras del combo (no suman). Base debe existir.
  - **Compra/Ajuste**: `SKU o Código` + `Cantidad` (+ negativo resta) + `Factura`; acepta SKU de COMBO (expande a base × cantidad); **vista previa obligatoria** antes de aplicar; registra movimientos.
  - **Combos SOLO actualizar (28/8)**: un Excel con foto/ubicación/código de barras y SIN armado antes se descartaba entero ("sin productos válidos"). Ahora, si el combo YA EXISTE (por SKU o por cualquier código de barras), esas filas lo ACTUALIZAN sin tocar su armado, y lo que el Excel no trae NO se pisa (nombre, códigos, precio, ubicación o tipo vacíos dejan lo cargado). Un combo nuevo sigue necesitando armado y se informa aparte. Resultado real 28/8 con el consolidado de 1526 filas y 1398 fotos: **1348 combos actualizados**, 38 no creados (no existen en la app).
  - **📐 Medidas ML (21/8, CORREGIDO 24/8 por aclaración del usuario)**: botón en Inventario que importa el **Reporte de Planificación de ML** (columnas CODIGO / SKU con varios MLA por coma / Tamaño "15 x 20 x 20"): el CODIGO se agrega como código de barras del combo o producto que matchee (un combo acumula varios códigos), y el Tamaño se guarda como `dims` del **PRODUCTO base** (si el SKU es combo, va a sus productos; las medidas son POR UNIDAD del producto). Aplica vía `bulkApplyPatches` en App (writeBatch de 400 + setState funcional una sola vez). Empaquetado calcula la bolsa con las medidas por unidad del producto (× cantidades); ya NO usa dims del combo. Botón "📄 Plantilla medidas" al lado. **AMPLIADO 28/8**: (a) columna **UBICACION** → se guarda en los PRODUCTOS base (donde está el combo está el producto que lo compone; si un producto está en varios combos queda la primera); (b) las **medidas solo se aplican si el SKU es UNA unidad de UN producto** — son POR UNIDAD y Empaquetado las multiplica por la cantidad, así que la caja de un pack de 10 (ej. MLA1184382887 "10 Pilotines" 18,2×13,8×5,8) haría sugerir una bolsa mucho más grande; los packs se saltean y se cuentan en el resumen; (c) alcanza con la columna SKU (CODIGO opcional) y la hoja con los datos se busca por sus encabezados en TODAS las hojas (antes leía solo la primera y una hoja de instrucciones adelante rompía la importación).
- **Movimientos**: tabs funcionales Todos/Entradas/Salidas/**Hoy/Ayer/Semana** + buscador + resumen entró/salió/neto; tarjetas con +verde/−rojo grande y SKU. Formulario (20/8): **buscador de producto con texto** (nombre/SKU/código de barras, máx 8 resultados, SOLO productos — los combos siguen entrando por Excel de compra o escaneo QR); el select viejo con combos se eliminó.
- **MercadoLibre (solapa ML)**: 2 cuentas conectadas por OAuth — **FULL** = app "Uniproveedores MCP" (client 5731065254303938), **FERRE** = app "Publicacion-por-telegram" (914895574262615). Redirect URI = preview largo con `/` final. En la app SIEMPRE loguearse como admin; el navegador define qué cuenta ML se autoriza. Sincronizar ventas (vista previa → descuenta stock, excluye `fulfillment`, mapea SKU/MLA → combo → base, marca ml_orders), **cron 2 veces por día: 16:00 y 21:00 UTC (13hs y 18hs AR — pedido del usuario 21/8; ml_orders evita doble descuento)** con **ventana móvil de 48hs** (antes usaba "medianoche del servidor" = 21hs AR del día anterior y las ventas de 18–21hs AR se perdían para siempre — bug corregido 16/8; ml_orders evita doble descuento) en producción stock-inventario (env: CRON_EMAIL/CRON_PASSWORD = usuario Firebase cron@distribuidorauniverso.com; la service account key está bloqueada por política de org, por eso login email/password), botón "Probar automático ahora", "Publicaciones faltantes" (Excel de MLA sin cargar).
- **Envíos (logística FLEX)**: ver bloque siguiente.
- **📦 Empaquetado** (`src/components/Packing.jsx`): flujo guiado de armado. Escanea QR de la venta (mismo matching flexible que Envíos) → **Paso 1**: banner de bolsa según medidas ML (`dims` del envío; bolsas REALES: verde 20×30 / blanca 30×40 / gris 40×50 cm, si no entra → empaque especial; criterio de encaje en bolsa plana: ancho+alto+2 ≤ ancho bolsa y largo+alto+2 ≤ largo bolsa). Si ML no manda medidas, se **estima con el campo "Medidas" del producto** (cm "largo x ancho x alto", nuevo en el formulario de Inventario 18/8): 1 artículo → apila unidades y prueba encaje; varios → suma volúmenes vs capacidad (cap: verde 1500 / blanca 4800 / gris 11000 cm³); banner dice "por tamaño de los artículos", aviso FRÁGIL si algún producto lo es (checkbox `fragile` nuevo en formulario de producto), checklist de artículos con foto/ubicación/cantidad (SKU combo se expande a productos base × cantidad). Al continuar pasa el envío a **Armado** automático. **Paso 2**: total de unidades gigante + checks de cantidades y papelitos de calificación/devolución. **Paso 3**: leyenda "pegá la etiqueta del lado LISO" + cuenta regresiva 6s que vuelve sola al escáner. Requiere que el envío tenga `items`/`dims` (los guarda el sync desde commit 75d6f6e; envíos anteriores muestran aviso — Vaciar+Traer una vez si se necesita el detalle en todos).
- **Dashboard (29/8, rediseñado a pedido)**: sin listados largos. Quedan solo **2 listas de 5**: "Ingresos de hoy" (los 5 mayores ingresos de stock del día, con el producto) y "Los 5 más bajos de stock". Tarjetas: Stock total se mantiene y "Movimientos" pasó a llamarse **"Unidades vendidas"** — y CUENTA SOLO VENTAS (`esVenta`: motivo que empieza con "venta" o referencia "ML"); antes sumaba también los ajustes manuales de stock y marcaba 4.000+ unidades en un día, error que detectó el usuario.
- **Inventario en PC (30/8)**: la tabla quedaba tan ancha que no se veía la línea completa ni los botones. Bloque CSS **solo PC** (`@media (min-width: 900px)`) en Inventory.css con anchos por columna, tipografía y padding reducidos; en celular no cambió nada.
- **Fotos (2/9)**: el input de foto del producto ofrece **cámara y galería** (antes abría solo la galería). Las miniaturas y la foto grande usan `object-fit: contain` sobre fondo blanco (antes `cover`, que recortaba/estiraba los productos).
- **Combos: ubicación y foto del producto base (1-2/9)** — `ubicacionCombo` / `fotoCombo` en Inventory.jsx. El combo que no tiene ubicación propia muestra la de su(s) producto(s) base (si son dos, las dos separadas por ·) y, si no tiene foto propia, muestra la del primer producto base que tenga. **Se calcula EN VIVO al dibujar la lista, no se guarda nada**: apenas se le carga foto/ubicación a un producto, todos sus combos la muestran al instante → **NO hace falta ninguna tarea diaria de "actualizar fotos y ubicación"** (el usuario la pidió el 3/9 y se descartó por esto). Si un combo igual aparece "Sin ubicación" y sin foto es porque **no tiene armado cargado** (sin productos base no hay de dónde heredar; se ve además como "Stock: 0 armables") o porque su base todavía no tiene foto/ubicación. Ojo al tocar el código: a Editar/Duplicar hay que pasarles `comboOriginal(row)` para no guardar los valores heredados como propios.
- **PRIMER EMPAQUE (3/9)**: checkbox `primerEmpaque` en el formulario del producto base, **independiente de FRÁGIL** (pueden estar los dos). Empaquetado muestra un banner azul "📦 PRIMER EMPAQUE — envolver el artículo antes de meterlo en la bolsa" si alguna línea del pedido lo tiene, incluidas las que salen de expandir un combo.
- **📊 Métricas (30-31/8, `src/components/Metrics.jsx`)**: réplica de los Indicadores de ML. Chips de cuenta (**Ambas / FULL / FERRE**) y de período (Hoy / 7 / 15 / 30 días) + checkbox "con envíos" (deshabilitado en 15 y 30 días porque pedir el tipo de envío de cada orden se hace lento). Backend `api/ml/exchange.js` `action=metrics`: agrega por día EN HORA ARGENTINA, desglosa tipo de envío, trae visitas (`/users/{id}/items_visits`), reputación (`/users/me`) y saldo de Mercado Pago. KPIs + **gráfico de evolución con alturas en píxeles** (`ALTO_BARRA=170`; con alturas en % las barras no se veían) + tablas. El cuadro de **Mercado Pago** muestra **dinero total, disponible y la diferencia = a liquidar** (corrección del usuario). Token compartido con la solapa ML vía `src/utils/mlToken.js` (`ensureMlToken`). La última tabla va dentro de `.mt-table-wrap` con scroll horizontal para que entre en el celular.
- **🏆 Top 200 más vendidos (30/8)**: `action=topsold` en exchange.js — ventas de FULL+FERRE del último mes (moto, correo y Full), ventanas semanales y paginado en paralelo (el `/orders/search` de ML corta en offset 10.000), Excel con los 200 SKU más vendidos para priorizar medida/foto/ubicación.
- **💰 Finanzas (1/9, `src/components/Finance.jsx` + `.css`, ~600 líneas)**: el panel de control financiero que el usuario tenía en un HTML suelto, portado a React + Firestore (colección **`finanzas`**, solo admin). Calendario con modal por día, alta de registro (tipo/fecha/moneda/monto/proveedor/remito/factura/descripción), tres listados con marcar pagado y borrar, **cuenta corriente por proveedor con aplicación FIFO de pagos y días de atraso**, resumen mensual con gráfico y saldo acumulado, editor de cotización del dólar e **importador** de los datos del panel viejo (valida tipo/fecha/monto y deduplica por `origenId`); se generó `exportar-panel-viejo.html` para sacar los registros del localStorage del navegador. Los datos se cargan a mano (no hay integración bancaria).
- **Reportes** y Dashboard con mapa del depósito (settings, solo admin).

- **App instalable (ícono en el celular, 28/8)**: el manifest tenía un ícono SVG data-URI que Chrome no acepta, así que nunca ofrecía instalar. Ahora `public/icon-192.png|512|180` (📦 sobre fondo azul, generados con Chromium headless), manifest en español con `orientation: portrait`, apple-touch-icon y banner "📲 Instalar la app" en App.jsx (`beforeinstallprompt` guardado en estado; no aparece si ya está instalada). Abre en standalone, sin barra del navegador.

## Envíos — estado final
- Entran SOLOS desde ML: al abrir + **frecuencia por horario AR (24/8)**: 0-8hs cada 4hs · 8-10 cada 1h · 10-12 cada 15min · 12-18 cada 30min · 18-24 cada 3hs (setTimeout encadenado con `syncDelayMs`, tz America/Argentina/Buenos_Aires) + botón (con leyenda "esperá" animada SIEMPRE, incluso auto). **El sync corre EN EL SERVIDOR desde el 24/8** (`api/ml/envios.js`, credenciales cron@, maxDuration 300): CUALQUIER sesión abierta lo dispara (también los ayudantes y la pantalla del puesto — ya no depende de la sesión del master ni expone tokens); candado en `settings/enviosSync` (lastRunAt: 8 min auto, 90s manual con force) + lastSummary; hace todo (importa FLEX+correo, refresca estados, revisa viejos, backfill tracking) y el cliente llama al endpoint y recarga con `reloadShipments` de App. La lógica vieja del cliente se eliminó de Shipments.jsx (reutiliza `fetchOrdersDetailed` exportado de exchange.js). El TABLERO es solo `self_service` (FLEX/Turbo); excluye Full/retiro/cancelados. **CORREO (cross_docking/drop_off) se importa aparte SOLO para Empaquetado (24/8)**: entra con `channel:'correo'` + status archivado — App filtra `flexShipments` (channel≠correo) para el tablero y pasa `allShipments` a Shipments (dedup del sync) y el listado completo a Packing, que muestra chip 📮 CORREO y encuentra la venta al escanear. **UN envío por compra** (packId || shipmentId). Trae destinatario, dirección, coords → pin automático.
- Ventana **7 días** (usuario mueve ~300 envíos/día → backend trae hasta **3000 órdenes/cuenta con detalle de envíos en paralelo, lotes de 10**). Estado inicial: delivered→Entregado (con fecha), shipped ≤48hs→En camino, más viejo o not_delivered→Demorado, abierto ≤48hs→Pendiente, abierto viejo→Demorado. El sync agrega nuevos (dedup por seen) y además **refresca los ya cargados contra ML** (18/8): delivered→Entregado, not_delivered→Demorado, en camino >48hs→Demorado, **cancelled (orden o envío)→Archivado** (21/8: cancelación del comprador saca el envío del panel; también se evita importarlos); armado/motoquero puestos a mano NO se tocan. Los activos **fuera de la ventana de 7 días** (pendientes viejos colgados) se consultan uno por uno vía `action=shipstatus` (21/8, hasta 400 por sync, por cuenta y reintentando en la otra si no tiene `account`): delivered→Entregado, cancelled→Archivado, not_delivered/shipped viejos→Demorado. **No usar Vaciar** salvo reset total (pierde gestión).
- Flujo: Pendiente de imprimir → Armado → En camino (auto al asignar motoquero SOLO desde pendiente/armado; un DEMORADO sigue demorado al asignarle moto — si no "desaparecía" del filtro) → Entregado → **Archivado**. Filtro de motoqueros muestra las cantidades del día de cada uno: asignados (✅ entregados · 🛵 pendientes) — desde 25/8 cuenta SOLO lo asignado hoy vía `courierAssignedAt` (sello real de asignación, se guarda en cada assignCourier; assignedAt puede traer la fecha de la venta de ML y los entregados-confirmados-hoy inflaban el número — caso Gerardo 192 vs 122 reales). (sale de mapa y de "Todos"; filtro propio; sigue contando como entregado en reporte). QR de etiqueta FLEX solo BUSCA (parse JSON id / dígitos, matching flexible); si no encuentra muestra el contenido del QR. **Pistola/lector QR HID soportado (19/8)** en Envíos y Empaquetado: listener global de teclado detecta tipeo <50ms/tecla terminado en Enter (≥8 chars) → handleScan; en Envíos se ignora si el foco está en un input. OJO (21/8): el **código de barras 1D de la etiqueta trae el `tracking_number`**, DISTINTO del shipmentId del QR — se guarda `trackingNumber` en cada envío (sync nuevo + backfill en refresh/shipstatus) y el matching del escaneo lo reconoce; así una pistola solo-1D funciona igual.
- Mapa: en "Todos" solo activos del día; **al elegir un filtro de estado muestra ese estado**; búsqueda y motoquero también filtran el mapa. Leyenda de colores solo PC (rojo pendiente / naranja armado / azul camino).
- Barra de búsqueda (código/pack/destinatario/dirección/motoquero) con acciones en la tarjeta. Costo NO está en el tablero.
- **🗺️ Ruta por motoquero (21/8, panel solo PC en tablero)**: elegí motoquero → "Armar mejor ruta" sobre sus envíos EN CAMINO con coordenadas: primero los con restricción horaria (`timeWindowOf` detecta "horario comercial"→18 o rangos "de 9 a 18"/"hasta las 17" en `notes` —la Referencia de la etiqueta, guardada desde 21/8— o dirección), ordenados por vencimiento y cercanía; después el resto por vecino más próximo desde `DEPOT` = **Bacacay 4726, Floresta (-34.6309, -58.5004, aprox por cuadra)**; el link de Maps del Tramo 1 va SIN origen → Google arranca desde la ubicación actual del motoquero (origen variable, pedido 23/8). Links de Google Maps encadenados en tramos de 10 paradas + botón WhatsApp (wa.me con listado y links) + Copiar. Sin ubicación quedan listados aparte.
- **Reporte (solo PC, oculto en celular)**: hoy/7/30 días + **filtro por motoquero** y **cuadro resumen por motoquero** (envíos/entregados/demorados/demora prom./cobro ML/pago — visible con "Todos" y hoja 2 "Por motoquero" en el Excel); por envío: **Zona FLEX** (cercana $4.490 / media $6.490 / lejana $8.690 / muylejana $9.990 — constantes ZONES en Shipments.jsx, actualizar cuando ML cambie) con **zona automática**: primero por la localidad de la etiqueta ML (`zoneOf`: dirección con "Capital Federal"/"CABA" = cercana), si no por coordenadas (CABA_POLY + anillos desde el Obelisco ≤20km media, ≤38km lejana, más = muylejana); `effZone` = s.zone guardada pisa el cálculo; columna Zona muestra 📍 localidad (`locOf` = dirección después de la calle) y el Excel tiene columna Localidad → Cobro ML auto; **Pago motoquero automático por zona COMPLETO** (19/8: cercana $2.750 / media $4.500 / lejana $6.000 / muylejana $8.000; lo cargado a mano pisa lo automático) y **Pagó comprador** editable; totales + export Excel.
- **Auto-archivo (1/9, `api/ml/envios.js`)**: un envío **entregado hace más de 7 días pasa solo a Archivado** (`autoArchivados` en el resumen del sync) y los **archivados de más de 1 mes ya no se cuentan** — `tablero` (memo en Shipments.jsx) los saca del listado, del mapa y de los contadores, pero siguen en Firestore y en el reporte.
- **Reporte: rango de fechas libre (31/8)**: además de hoy/7/30 días hay **Desde / Hasta**; si se completan pisan los rangos rápidos y el Excel sale como `envios-<desde>_a_<hasta>.xlsx`. El usuario pidió que el reporte guarde 2 años de historia: **postergado por él mismo** ("deja el reporte así como está, en 2 años vemos") — hoy no hay purga automática de shipments.
- **Escáner (30/8, causa raíz)**: la cámara se reiniciaba sola y la app "se tildaba" al asignar motoquero porque el efecto de `Scanner.jsx` dependía de `onScan`, que se recrea en cada render del padre. Ahora `onScanRef` + `useEffect(..., [])`: la cámara arranca UNA vez. **Secuencia oficial definida por el usuario**: escaneo con pistola o cámara → el escaneo **solo trae el envío, no modifica nada** → en pantalla se marca **Armado** a mano → se le asigna motoquero → pasa a **En camino**. No automatizar pasos intermedios (un intento anterior de cerrar/reabrir paneles solo "quedó peor").
- Lección técnica: setShipments/setCouriers SIEMPRE en forma funcional + shipmentsLiveRef para dup-check (los adds en ráfaga se pisaban y parecía que nada guardaba).

## Pendiente (no hecho)
1. ~~Devoluciones de ML que SUMAN stock~~ **DESCARTADO 31/8 por decisión del usuario**: las devoluciones se controlan a mano porque hay que revisar si el producto vuelve vendible o es descarte, así que NO se automatiza el reingreso al stock. Si vuelve a pedirse, la entrada se carga por Movimientos o por Compra/Ajuste como cualquier otra.
2. **Cargar stock real** de productos base (el usuario lo va ajustando con Compra/Ajuste).
3. ~~Stock desde foto de factura~~ **HECHO 21/8**: botón "📷 Foto factura" en Inventario (junto a Compra/Ajuste) → comprime la foto en el navegador (1600px jpg) → `/api/contabilium?action=extract` (Gemini 2.5 Flash, GEMINI_API_KEY cargada en Vercel por el usuario, maxDuration 60) → items {codigo, descripcion, cantidad} → matching: código exacto (producto/combo con expansión) y si no, por descripción (fuzzy: ≥2 palabras y ≥50% en el nombre) → misma vista previa de compra → confirmar. No matcheados se listan y se cargan a mano.
4. Envíos a bodega **Full** (descuento al enviar).
4b. **Mejorar la foto de factura** (24/8: el usuario dijo "algo ayudó, lo mejoramos en unos días" — retomar con sus ejemplos reales de qué matcheó mal). La ruta por WhatsApp y la pistola (por código de barras) YA FUNCIONARON, confirmado por el usuario 24/8. Botón "📄 Plantilla medidas" agregado junto a 📐 Medidas ML.
5. Si se quiere usar el dominio corto para conectar ML: agregar `https://stock-inventario-sable.vercel.app/` como Redirect URI en ambas apps ML y reconectar.
6. Valores de zonas FLEX editables desde la app (hoy constantes).
7. ~~Verificar el cron~~ **VERIFICADO 28/8** con "Probar automático ahora" (ver regla de descuento más abajo).
9. **Publicaciones de ML: 100% cargadas al 28/8** — "Todas las publicaciones ya están cargadas": FULL 2078, FERRE 1546. Se llegó importando los Excel de "🔍 Publicaciones faltantes" (el de FULL, 734 combos, revisado y ordenado en el chat: filas de continuación sin SKU, cantidades vacías y una hoja "Revisar" con las filas que faltaba completar). Repetir el botón cada tanto para publicaciones nuevas.
11. **Archivos preparados en el chat (28/8, esperando resultado del usuario)**: `combos-fotos-listo.xlsx` (YA IMPORTADO, 1348 ok) y `medidas-y-ubicacion.xlsx` (2761 filas: 2041 SKU con medidas del paquete sacadas del export de publicaciones de ML —columnas SELLER_PACKAGE_HEIGHT/LENGTH/WIDTH y, si faltaban, los atributos "alto/ancho/largo del paquete"— y 1346 con ubicación del consolidado; se sube por 📐 Medidas ML). Pendiente: el usuario lo importa y pasa el resumen.
12. Quedan **38 publicaciones del consolidado sin cargar** (41 filas sin SKU + 1 de prueba "Algo"/"Nose"): necesitan SKU de ML y armado.
13. Al ordenar Excel con fotos NO regenerar el archivo con SheetJS (borra las imágenes): hay que editar el XML dentro del .xlsx y NO mover filas — las fotos se anclan por número de fila (`xl/drawings/drawing1.xml`). Así se hizo el consolidado (1398 fotos intactas, verificado comparando anclaje por anclaje).
10. La lectura de **foto de factura** se cortaba y devolvía "IA no devolvió JSON válido": gemini-2.5-flash piensa por defecto y esos tokens consumían el tope de salida de 4000 → ahora `thinkingConfig.thinkingBudget: 0` y tope 32000, y el error distingue el corte por longitud (28/8).
8. Marcar productos frágiles (checkbox nuevo en Inventario) para que Empaquetado avise.

14. **PENDIENTE DEL USUARIO — publicar `firestore.rules` en Firebase** (Console → Firestore Database → pestaña Reglas → pegar el archivo del repo → Publicar). Sin eso la solapa 💰 Finanzas no puede leer ni escribir la colección `finanzas` (regla nueva: `match /finanzas/{id} { allow read, write: if isAdmin(); }`).
15. Pendientes del usuario (le tocan a él, no al código): importar `medidas-y-ubicacion.xlsx` por 📐 Medidas ML y pasar el resumen · correr "🏆 Top 200 más vendidos" y revisar medida/foto/ubicación de esos SKU · exportar el panel financiero viejo con `exportar-panel-viejo.html` e importarlo en Finanzas · cargar las 38 publicaciones que faltan.

## Reglas de negocio clave (definidas por el usuario)
- Stock: descuenta TODO lo despachado desde el depósito propio (FLEX+correo/colecta+ventas sin envío de ML), NUNCA `fulfillment` (Full ya salió al enviarse a bodega). Igual para ambas cuentas. 2 veces/día (13 y 18hs AR). Stock puede quedar negativo. **Clasificación explícita desde el 28/8** (`clasificar`/`descuenta` en api/ml/cron.js): la regla vieja era "todo lo que no sea fulfillment descuenta", así que si fallaba el pedido del envío a ML el tipo quedaba nulo y la venta se descontaba igual — incluidas las de Full — y además no se miraba el estado de la orden (canceladas y sin cobrar también descontaban; caso real: 771 Full + 60 canceladas en 48hs de la cuenta FULL). Ahora: descuentan flex (`self_service`) + correo (`drop_off`/`xd_drop_off`/`cross_docking`) + sin envío, con estado `paid`/`partially_paid`; NO descuentan Full, canceladas/inválidas, sin cobrar (quedan para la corrida siguiente) ni las de tipo desconocido (se reintenta el pedido a ML y si igual no se sabe NO se descuenta: preferimos que falte un descuento antes que descontar una de Full). El resumen del botón "Probar automático ahora" muestra el desglose. Verificado 28/8 en producción: FULL 168 flex + 2 correo (771 Full salteadas — es correcto, ~98% de esa cuenta va por bodega), FERRE 103 flex + 83 correo + 6 sin envío.
- Reparto (Envíos): SOLO FLEX/Turbo (motos propias). Por venta/cliente, no por producto. Historial 7 días para ver evolución; demorados hasta 1 semana.
- ML paga por zona (bonificación); al motoquero se le paga otro monto; el comprador puede haber pagado o no el envío — 3 columnas separadas en el reporte.
